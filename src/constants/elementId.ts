/**
 * 엘리먼트의 ID를 상수화합니다.
 */

const common = {
  HEADER: 'header',
};

const productDetail = {
  REVIEW: 'review',
};

const search = {
  INPUT: 'search-input',
};

export const elementId = {
  productDetail,
  common,
  search,
} as const;
